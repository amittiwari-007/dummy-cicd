pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                // Add build commands (e.g., Maven, Gradle, npm)
            }
        }

        stage('Test') {
            steps {
                // Add test commands
            }
        }

        stage('Deploy') {
            steps {
                // Add deployment commands (e.g., copy files, deploy to server)
            }
        }
    }
}
